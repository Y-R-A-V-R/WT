const displayData = require('./displayData'); // Adjust path if needed

const myString = "This is some extra information.";
displayData.displayInfo(myString);
const displayData = require('./displayData'); // Adjust path if needed

const mytring = "This is some extra information.";
displayData.displayInfo(mytring);
